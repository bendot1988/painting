[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Quote form submissions trigger netlify/functions/submission-created.mjs
# which emails via Resend (RESEND_* env vars in Netlify UI).
